{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "echo '📝 파일 수정 완료' && echo '🔍 수정된 패키지 검증 중...' && pnpm turbo lint type-check --filter=...[HEAD] --continue || echo '⚠️ 검증 실패 - 수정이 필요합니다'"
          }
        ]
      }
    ]
  }
}